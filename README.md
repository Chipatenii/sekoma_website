# sekoma_website

Static website for Sekoma Energy Limited.

## Front-end CSS

Tailwind is compiled locally so the live site does not rely on the browser CDN compiler.

```bash
npm install
npm run build:css
```

The generated stylesheet is `assets/tailwind.css`. Custom design polish lives in `assets/site.css`.
